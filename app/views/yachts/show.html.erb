<div class="container">
  <h1>Yacht Details:</h1>
  <div class="row">
    <div class="col-md-6">
      <h2><%= @yacht.name %></h2>
      <p><strong>Number of Guests:</strong> <%= @yacht.number_of_guests %></p>
      <p><strong>Price:</strong> <%= @yacht.price %>€</p>
      <p><strong>Location:</strong> <%= @yacht.location %></p>
      <p><strong>Owner:</strong> <%= @yacht.user.username %></p>
    </div>
  </div>
  <div class="row mt-3">
    <div class="col-md-12">
      Details
    </div>
  </div>
  <div class="row mt-3">
    <div class="col-md-12">
      <%= link_to "Back to Yachts List", yachts_path, class: "btn btn-primary" %>
      <%= link_to "Edit", edit_yacht_path(@yacht), class: "btn btn-primary" %>
      <%= button_to "Delete", @flat, class: "btn btn-primary mt-3", method: :delete%>
    </div>
  </div>
</div>
